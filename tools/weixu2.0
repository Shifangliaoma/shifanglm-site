<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>十方料码 - 专业彩票数据分析工具</title>
    <style>
        :root {
            --primary-gold: #FFD700;
            --primary-red: #DC2626;
            --primary-blue: #1E40AF;
            --primary-green: #10B981;
            --brand-gradient: linear-gradient(135deg, #FFD700 0%, #DC2626 50%, #1E40AF 100%);
            --button-initial: #4B5563;
            --button-active: #DC2626;
            --button-hover: #374151;
            --dark-bg: #0F172A;
            --card-bg: rgba(30, 41, 59, 0.9);
            --text-primary: #F8FAFC;
            --text-secondary: #94A3B8;
            --border-color: rgba(255, 215, 0, 0.2);
            --brand-shadow: 0 0 20px rgba(255, 215, 0, 0.3);
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Microsoft YaHei', sans-serif;
            background: var(--dark-bg);
            color: var(--text-primary);
            line-height: 1.6;
            padding: 16px;
            min-height: 100vh;
            background-image: 
                radial-gradient(circle at 10% 20%, rgba(255, 215, 0, 0.05) 0%, transparent 20%),
                radial-gradient(circle at 90% 80%, rgba(220, 38, 38, 0.05) 0%, transparent 20%);
        }
        
        .container {
            max-width: 800px;
            margin: 0 auto;
        }
        
        .brand-header {
            text-align: center;
            margin-bottom: 24px;
            padding: 20px 0;
            position: relative;
        }
        
        .brand-logo {
            font-size: 3rem;
            font-weight: 900;
            margin-bottom: 10px;
            background: var(--brand-gradient);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            text-shadow: var(--brand-shadow);
            letter-spacing: 2px;
        }
        
        .brand-subtitle {
            color: var(--primary-gold);
            font-size: 1.2rem;
            font-weight: 500;
            margin-bottom: 8px;
            letter-spacing: 1px;
        }
        
        .brand-tagline {
            color: var(--text-secondary);
            font-size: 0.9rem;
            opacity: 0.8;
        }
        
        .contact-info {
            background: linear-gradient(135deg, rgba(255, 215, 0, 0.1), rgba(220, 38, 38, 0.1));
            border-radius: 10px;
            padding: 15px;
            margin: 20px auto;
            text-align: center;
            border: 1px solid var(--border-color);
            max-width: 500px;
        }
        
        .wechat-info {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
            margin-top: 10px;
            font-size: 1.1rem;
            color: var(--primary-gold);
            font-weight: 600;
        }
        
        .wechat-icon {
            font-size: 1.5rem;
            animation: pulse 2s infinite;
        }
        
        .card {
            background: var(--card-bg);
            border-radius: 12px;
            padding: 20px;
            margin-bottom: 16px;
            border: 1px solid var(--border-color);
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.3);
            position: relative;
            overflow: hidden;
        }
        
        .card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 3px;
            background: var(--brand-gradient);
        }
        
        .section-title {
            font-size: 1.2rem;
            font-weight: 700;
            margin-bottom: 16px;
            color: var(--primary-gold);
            display: flex;
            align-items: center;
            gap: 8px;
        }
        
        .section-title::before {
            content: '✦';
            color: var(--primary-red);
        }
        
        .textarea {
            width: 100%;
            height: 120px;
            padding: 12px;
            border: 1px solid var(--border-color);
            border-radius: 8px;
            background: rgba(15, 23, 42, 0.7);
            color: var(--text-primary);
            resize: vertical;
            margin-bottom: 12px;
            font-family: 'Courier New', monospace;
            font-size: 14px;
        }
        
        .textarea:focus {
            outline: none;
            border-color: var(--primary-gold);
            box-shadow: 0 0 0 2px rgba(255, 215, 0, 0.2);
        }
        
        .stats {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 12px;
            margin: 16px 0;
        }
        
        .stat-item {
            background: rgba(255, 215, 0, 0.05);
            padding: 12px;
            border-radius: 8px;
            text-align: center;
            border: 1px solid var(--border-color);
        }
        
        .stat-label {
            font-size: 12px;
            color: var(--text-secondary);
            margin-bottom: 4px;
        }
        
        .stat-value {
            font-size: 18px;
            font-weight: 700;
            color: var(--primary-gold);
        }
        
        .btn-group {
            display: flex;
            gap: 8px;
            flex-wrap: wrap;
        }
        
        .btn {
            padding: 12px 16px;
            border: none;
            border-radius: 8px;
            font-size: 14px;
            font-weight: 600;
            cursor: pointer;
            flex: 1;
            min-width: 120px;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }
        
        .btn::after {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            width: 5px;
            height: 5px;
            background: rgba(255, 255, 255, 0.5);
            opacity: 0;
            border-radius: 100%;
            transform: scale(1, 1) translate(-50%);
            transform-origin: 50% 50%;
        }
        
        .btn:active::after {
            animation: ripple 1s ease-out;
        }
        
        .btn:active {
            transform: translateY(1px);
        }
        
        .btn-primary {
            background: linear-gradient(135deg, var(--primary-blue), #3730A3);
            color: white;
            border: 1px solid rgba(59, 130, 246, 0.5);
        }
        
        .btn-success {
            background: linear-gradient(135deg, var(--primary-green), #047857);
            color: white;
            border: 1px solid rgba(16, 185, 129, 0.5);
        }
        
        .btn-brand {
            background: linear-gradient(135deg, var(--primary-red), #9F1239);
            color: white;
            border: 1px solid rgba(220, 38, 38, 0.5);
        }
        
        .match-container {
            display: grid;
            grid-template-columns: 1fr;
            gap: 12px;
            margin: 16px 0;
        }
        
        .match-row {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 8px;
        }
        
        .match-option {
            padding: 12px 8px;
            border: 2px solid var(--button-initial);
            border-radius: 6px;
            background: rgba(75, 85, 99, 0.5);
            color: white;
            text-align: center;
            cursor: pointer;
            font-weight: 700;
            transition: all 0.3s ease;
        }
        
        .match-option:hover {
            background: var(--button-hover);
            border-color: var(--button-hover);
            transform: translateY(-2px);
        }
        
        .match-option.selected {
            background: linear-gradient(135deg, var(--primary-red), #B91C1C);
            border-color: var(--primary-gold);
            color: white;
            box-shadow: 0 0 15px rgba(220, 38, 38, 0.6);
            transform: scale(1.05);
        }
        
        .double-match-section {
            margin-top: 20px;
            padding: 20px;
            background: rgba(255, 215, 0, 0.05);
            border-radius: 8px;
            border: 1px solid var(--border-color);
        }
        
        .double-match-title {
            font-size: 1.1rem;
            font-weight: 700;
            margin-bottom: 16px;
            color: var(--primary-gold);
            text-align: center;
        }
        
        .position-options {
            display: flex;
            justify-content: center;
            gap: 12px;
            margin-bottom: 16px;
        }
        
        .position-btn {
            padding: 10px 16px;
            background: rgba(75, 85, 99, 0.5);
            color: white;
            border: 2px solid var(--button-initial);
            border-radius: 6px;
            font-size: 14px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
        }
        
        .position-btn:hover {
            background: var(--button-hover);
            border-color: var(--button-hover);
            transform: translateY(-2px);
        }
        
        .position-btn.selected {
            background: linear-gradient(135deg, var(--primary-red), #B91C1C);
            border-color: var(--primary-gold);
            color: white;
            box-shadow: 0 0 10px rgba(220, 38, 38, 0.5);
            transform: scale(1.05);
        }
        
        .match-type-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 8px;
            margin-bottom: 16px;
        }
        
        .type-btn {
            padding: 8px 12px;
            background: rgba(75, 85, 99, 0.5);
            color: white;
            border: 2px solid var(--button-initial);
            border-radius: 4px;
            font-size: 12px;
            font-weight: 600;
            cursor: pointer;
            text-align: center;
            transition: all 0.3s ease;
        }
        
        .type-btn:hover {
            background: var(--button-hover);
            border-color: var(--button-hover);
            transform: translateY(-1px);
        }
        
        .type-btn.selected {
            background: linear-gradient(135deg, var(--primary-red), #B91C1C);
            border-color: var(--primary-gold);
            color: white;
            box-shadow: 0 0 8px rgba(220, 38, 38, 0.5);
            transform: scale(1.05);
        }
        
        .tolerance-settings {
            display: grid;
            grid-template-columns: 1fr auto 1fr;
            gap: 8px;
            align-items: end;
            margin: 16px 0;
        }
        
        .number-input {
            width: 100%;
            padding: 8px;
            border: 1px solid var(--border-color);
            border-radius: 4px;
            background: rgba(15, 23, 42, 0.7);
            color: var(--text-primary);
            text-align: center;
            font-weight: 600;
        }
        
        .number-input:focus {
            outline: none;
            border-color: var(--primary-gold);
            box-shadow: 0 0 0 2px rgba(255, 215, 0, 0.2);
        }
        
        .result-section {
            margin-top: 20px;
            padding: 16px;
            background: rgba(255, 215, 0, 0.05);
            border-radius: 8px;
            border: 1px solid var(--border-color);
        }
        
        .result-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 12px;
        }
        
        .result-count {
            font-size: 20px;
            font-weight: 800;
            color: var(--primary-gold);
            text-shadow: 0 0 10px rgba(255, 215, 0, 0.5);
        }
        
        .verification-input {
            width: 100%;
            padding: 12px;
            border: 1px solid var(--border-color);
            border-radius: 6px;
            background: rgba(15, 23, 42, 0.7);
            color: var(--text-primary);
            text-align: center;
            margin-bottom: 12px;
            font-size: 18px;
            font-weight: 700;
            letter-spacing: 5px;
        }
        
        .verification-input:focus {
            outline: none;
            border-color: var(--primary-gold);
            box-shadow: 0 0 0 3px rgba(255, 215, 0, 0.2);
        }
        
        .verification-result {
            margin-top: 12px;
            padding: 16px;
            border-radius: 8px;
            text-align: center;
            font-size: 18px;
            font-weight: 800;
            opacity: 0;
            transform: scale(0.8);
            transition: all 0.5s ease;
            min-height: 60px;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        
        .verification-result.show {
            opacity: 1;
            transform: scale(1);
        }
        
        .verification-result.win {
            background: linear-gradient(135deg, rgba(16, 185, 129, 0.2), rgba(5, 150, 105, 0.2));
            color: var(--primary-green);
            border: 2px solid var(--primary-green);
            animation: pulse-win 1.5s ease-in-out;
        }
        
        .verification-result.lose {
            background: linear-gradient(135deg, rgba(220, 38, 38, 0.2), rgba(185, 28, 28, 0.2));
            color: var(--primary-red);
            border: 2px solid var(--primary-red);
            animation: pulse-lose 1.5s ease-in-out;
        }
        
        @keyframes pulse-win {
            0% { transform: scale(1); box-shadow: 0 0 0 0 rgba(16, 185, 129, 0.7); }
            50% { transform: scale(1.05); box-shadow: 0 0 0 15px rgba(16, 185, 129, 0); }
            100% { transform: scale(1); box-shadow: 0 0 0 0 rgba(16, 185, 129, 0); }
        }
        
        @keyframes pulse-lose {
            0% { transform: scale(1); box-shadow: 0 0 0 0 rgba(220, 38, 38, 0.7); }
            50% { transform: scale(1.05); box-shadow: 0 0 0 15px rgba(220, 38, 38, 0); }
            100% { transform: scale(1); box-shadow: 0 0 0 0 rgba(220, 38, 38, 0); }
        }
        
        @keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.1); }
            100% { transform: scale(1); }
        }
        
        @keyframes ripple {
            0% { transform: scale(0, 0); opacity: 1; }
            100% { transform: scale(20, 20); opacity: 0; }
        }
        
        .global-stats-grid {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 8px;
            margin-bottom: 20px;
        }
        
        .stats-btn {
            padding: 10px 8px;
            background: rgba(75, 85, 99, 0.5);
            color: white;
            border: 2px solid var(--button-initial);
            border-radius: 6px;
            font-size: 12px;
            font-weight: 600;
            cursor: pointer;
            text-align: center;
            transition: all 0.3s ease;
        }
        
        .stats-btn:hover {
            background: var(--button-hover);
            border-color: var(--button-hover);
            transform: translateY(-2px);
        }
        
        .stats-btn.selected {
            background: linear-gradient(135deg, var(--primary-red), #B91C1C);
            border-color: var(--primary-gold);
            color: white;
            box-shadow: 0 0 10px rgba(220, 38, 38, 0.5);
            transform: scale(1.05);
        }
        
        .double-stats-container {
            display: flex;
            gap: 15px;
            margin-top: 15px;
        }
        
        .position-column {
            flex: 1;
            display: flex;
            flex-direction: column;
            gap: 8px;
        }
        
        .position-title {
            font-size: 14px;
            font-weight: 700;
            text-align: center;
            margin-bottom: 10px;
            color: var(--primary-gold);
        }
        
        .stats-vertical-btn {
            padding: 8px 12px;
            background: rgba(75, 85, 99, 0.5);
            color: white;
            border: 2px solid var(--button-initial);
            border-radius: 4px;
            font-size: 11px;
            cursor: pointer;
            text-align: center;
            transition: all 0.3s ease;
            font-weight: 600;
        }
        
        .stats-vertical-btn:hover {
            background: var(--button-hover);
            border-color: var(--button-hover);
            transform: translateY(-1px);
        }
        
        .stats-vertical-btn.selected {
            background: linear-gradient(135deg, var(--primary-red), #B91C1C);
            border-color: var(--primary-gold);
            color: white;
            box-shadow: 0 0 8px rgba(220, 38, 38, 0.5);
            transform: scale(1.05);
        }
        
        .group-list {
            max-height: 200px;
            overflow-y: auto;
            margin-top: 15px;
            padding: 12px;
            background: rgba(255, 215, 0, 0.05);
            border-radius: 6px;
            border: 1px solid var(--border-color);
        }
        
        .group-item {
            padding: 8px;
            margin: 4px 0;
            background: rgba(255, 255, 255, 0.05);
            border-radius: 4px;
            font-family: 'Courier New', monospace;
            font-size: 12px;
            text-align: center;
            border-left: 3px solid var(--primary-gold);
        }
        
        .stats-result {
            display: none;
            margin-top: 20px;
            padding: 16px;
            background: rgba(255, 215, 0, 0.05);
            border-radius: 8px;
            border: 1px solid var(--border-color);
        }
        
        .stats-title {
            font-weight: 800;
            margin-bottom: 8px;
            text-align: center;
            color: var(--primary-gold);
            font-size: 1.1rem;
        }
        
        .stats-groups {
            max-height: 200px;
            overflow-y: auto;
            padding: 12px;
            background: rgba(15, 23, 42, 0.3);
            border-radius: 6px;
        }
        
        .audio-toggle {
            position: fixed;
            top: 20px;
            right: 20px;
            z-index: 1000;
            background: linear-gradient(135deg, var(--primary-red), #B91C1C);
            color: white;
            border: none;
            border-radius: 50%;
            width: 50px;
            height: 50px;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 20px;
            box-shadow: 0 4px 15px rgba(220, 38, 38, 0.4);
            transition: all 0.3s ease;
            border: 2px solid var(--primary-gold);
        }
        
        .audio-toggle.muted {
            background: linear-gradient(135deg, var(--button-initial), #4B5563);
        }
        
        .audio-toggle:active {
            transform: scale(0.95);
        }
        
        .brand-watermark {
            position: fixed;
            bottom: 20px;
            right: 20px;
            color: rgba(255, 215, 0, 0.2);
            font-size: 1.2rem;
            font-weight: 900;
            writing-mode: vertical-rl;
            text-orientation: mixed;
            letter-spacing: 3px;
            z-index: 1;
        }
        
        .brand-footer {
            text-align: center;
            margin-top: 30px;
            padding: 20px;
            color: var(--text-secondary);
            font-size: 0.9rem;
            border-top: 1px solid var(--border-color);
        }
        
        .brand-highlight {
            color: var(--primary-gold);
            font-weight: 700;
        }
        
        /* 滚动条样式 */
        ::-webkit-scrollbar {
            width: 8px;
            height: 8px;
        }
        
        ::-webkit-scrollbar-track {
            background: rgba(15, 23, 42, 0.5);
            border-radius: 4px;
        }
        
        ::-webkit-scrollbar-thumb {
            background: linear-gradient(135deg, var(--primary-gold), var(--primary-red));
            border-radius: 4px;
        }
        
        ::-webkit-scrollbar-thumb:hover {
            background: linear-gradient(135deg, var(--primary-red), var(--primary-gold));
        }
        
        /* 响应式设计 */
        @media (max-width: 768px) {
            .match-row {
                grid-template-columns: repeat(2, 1fr);
            }
            
            .global-stats-grid {
                grid-template-columns: repeat(2, 1fr);
            }
            
            .double-stats-container {
                flex-direction: column;
            }
            
            .position-column {
                flex-direction: row;
                flex-wrap: wrap;
                justify-content: center;
            }
            
            .stats-vertical-btn {
                flex: 1;
                min-width: 100px;
            }
            
            .brand-logo {
                font-size: 2.2rem;
            }
            
            .position-options {
                flex-direction: column;
                align-items: center;
            }
            
            .position-btn {
                width: 80%;
            }
        }
        
        @media (max-width: 480px) {
            .match-row {
                grid-template-columns: 1fr;
            }
            
            .global-stats-grid {
                grid-template-columns: 1fr;
            }
            
            .brand-logo {
                font-size: 1.8rem;
            }
        }
    </style>
</head>
<body>
    <button class="audio-toggle" id="audioToggle" onclick="toggleAudio()">🔊</button>
    <div class="brand-watermark">十方料码</div>
    
    <div class="container">
        <!-- 品牌头部 -->
        <div class="brand-header">
            <div class="brand-logo">十方料码</div>
            <div class="brand-subtitle">伪序动态复式软件</div>
            <div class="brand-tagline">专业彩票数据分析 · 精准命中计算</div>
        </div>
        
        <!-- 联系方式 -->
        <div class="contact-info">
            <div>专业彩票数据分析 · 精准选号策略</div>
            <div class="wechat-info">
                <span class="wechat-icon">💬</span>
                <span>微信: shifangliaoma</span>
            </div>
        </div>
        
        <!-- 数据导入 -->
        <div class="card">
            <div class="section-title">📥 数据导入</div>
            <textarea class="textarea" id="importData" placeholder="请输入复式数据，每行一组&#10;格式示例：6547-7569-3604&#10;更多数据格式请咨询微信: shifangliaoma"></textarea>
            
            <div class="stats">
                <div class="stat-item">
                    <div class="stat-label">当前组数</div>
                    <div class="stat-value" id="groupCount">0</div>
                </div>
                <div class="stat-item">
                    <div class="stat-label">数据状态</div>
                    <div class="stat-value" id="dataStatus">未加载</div>
                </div>
            </div>
            
            <div class="btn-group">
                <button class="btn btn-brand" onclick="playSound('import'); calculator.importData()">导入数据</button>
                <button class="btn btn-primary" onclick="playSound('clear'); calculator.clearData()">清除数据</button>
            </div>
        </div>
        
        <!-- 全局匹配设置 -->
        <div class="card">
            <div class="section-title">🎯 全局匹配设置</div>
            <div class="match-container">
                <div class="match-row" id="matchRow1"></div>
                <div class="match-row" id="matchRow2"></div>
            </div>
            
            <div class="tolerance-settings">
                <input type="number" class="number-input" id="globalMin" value="0" min="0" max="100" placeholder="最小">
                <span style="color: var(--primary-gold); font-weight: 700;">到</span>
                <input type="number" class="number-input" id="globalMax" value="0" min="0" max="100" placeholder="最大">
            </div>
        </div>
        
        <!-- 双位匹配设置 -->
        <div class="card">
            <div class="section-title">🔧 双位匹配设置</div>
            <div class="double-match-section">
                <div class="double-match-title">选择双位组合</div>
                <div class="position-options">
                    <button class="position-btn" id="btnHt" onclick="playSound('select'); calculator.selectDoublePosition('ht')">百十位</button>
                    <button class="position-btn" id="btnHu" onclick="playSound('select'); calculator.selectDoublePosition('hu')">百个位</button>
                    <button class="position-btn" id="btnTu" onclick="playSound('select'); calculator.selectDoublePosition('tu')">十个位</button>
                </div>
                
                <div class="double-match-title">选择匹配类型</div>
                <div class="match-type-grid">
                    <button class="type-btn" onclick="playSound('select'); calculator.selectDoubleType('0')">匹配0位</button>
                    <button class="type-btn" onclick="playSound('select'); calculator.selectDoubleType('1')">匹配1位</button>
                    <button class="type-btn" onclick="playSound('select'); calculator.selectDoubleType('2')">匹配2位</button>
                    <button class="type-btn" onclick="playSound('select'); calculator.selectDoubleType('01')">匹配0-1位</button>
                    <button class="type-btn" onclick="playSound('select'); calculator.selectDoubleType('12')">匹配1-2位</button>
                    <button class="type-btn" onclick="playSound('select'); calculator.selectDoubleType('02')">匹配0-2位</button>
                </div>
                
                <div class="tolerance-settings">
                    <input type="number" class="number-input" id="doubleMin" value="0" min="0" max="100" placeholder="最小">
                    <span style="color: var(--primary-gold); font-weight: 700;">到</span>
                    <input type="number" class="number-input" id="doubleMax" value="0" min="0" max="100" placeholder="最大">
                </div>
            </div>
        </div>
        
        <!-- 计算按钮 -->
        <div class="card" style="text-align: center;">
            <button class="btn btn-success" onclick="playSound('calculate'); calculator.calculate()" style="min-width: 200px; font-size: 16px; padding: 15px; background: linear-gradient(135deg, var(--primary-gold), var(--primary-red));">开始计算</button>
        </div>
        
        <!-- 结果显示 -->
        <div class="card" id="resultsCard" style="display: none;">
            <div class="section-title">📊 计算结果</div>
            <div class="result-section">
                <div class="result-header">
                    <div>
                        <div style="font-size: 12px; color: var(--text-secondary);">符合条件的注数</div>
                        <div class="result-count" id="resultCount">0</div>
                    </div>
                    <button class="btn btn-brand" onclick="playSound('copy'); calculator.copyResults()">复制号码</button>
                </div>
                
                <input type="text" class="verification-input" id="winningNumber" placeholder="输入开奖号验证" maxlength="3" oninput="this.value=this.value.replace(/[^0-9]/g,'')">
                <button class="btn btn-brand" onclick="playSound('verify'); calculator.verifyWinning()" style="width: 100%; margin-bottom: 15px;">验证是否中奖</button>
                
                <div class="verification-result" id="verificationResult"></div>
            </div>
        </div>
        
        <!-- 底部命中统计 -->
        <div class="card">
            <div class="section-title">📈 底部命中统计提取</div>
            
            <!-- 全局统计匹配 -->
            <div style="margin-bottom: 20px;">
                <div style="font-size: 1.1rem; font-weight: 700; margin-bottom: 12px; color: var(--primary-gold); text-align: center;">全局统计匹配</div>
                <div class="global-stats-grid">
                    <button class="stats-btn" onclick="playSound('extract'); calculator.extractGlobalStats('0')">匹配0位</button>
                    <button class="stats-btn" onclick="playSound('extract'); calculator.extractGlobalStats('1')">匹配1位</button>
                    <button class="stats-btn" onclick="playSound('extract'); calculator.extractGlobalStats('2')">匹配2位</button>
                    <button class="stats-btn" onclick="playSound('extract'); calculator.extractGlobalStats('3')">匹配3位</button>
                    <button class="stats-btn" onclick="playSound('extract'); calculator.extractGlobalStats('01')">匹配01位</button>
                    <button class="stats-btn" onclick="playSound('extract'); calculator.extractGlobalStats('12')">匹配12位</button>
                    <button class="stats-btn" onclick="playSound('extract'); calculator.extractGlobalStats('23')">匹配23位</button>
                    <button class="btn btn-brand" onclick="playSound('copy'); calculator.copyGlobalStats()" style="grid-column: span 1;">复制提取</button>
                </div>
            </div>
            
            <!-- 双位统计匹配 -->
            <div>
                <div style="font-size: 1.1rem; font-weight: 700; margin-bottom: 12px; color: var(--primary-gold); text-align: center;">双位统计匹配</div>
                <div class="double-stats-container">
                    <div class="position-column">
                        <div class="position-title">百十位</div>
                        <button class="stats-vertical-btn" onclick="playSound('extract'); calculator.extractDoubleStats('ht-0')">匹配0位</button>
                        <button class="stats-vertical-btn" onclick="playSound('extract'); calculator.extractDoubleStats('ht-1')">匹配1位</button>
                        <button class="stats-vertical-btn" onclick="playSound('extract'); calculator.extractDoubleStats('ht-2')">匹配2位</button>
                        <button class="stats-vertical-btn" onclick="playSound('extract'); calculator.extractDoubleStats('ht-01')">匹配01位</button>
                        <button class="stats-vertical-btn" onclick="playSound('extract'); calculator.extractDoubleStats('ht-12')">匹配12位</button>
                    </div>
                    
                    <div class="position-column">
                        <div class="position-title">百个位</div>
                        <button class="stats-vertical-btn" onclick="playSound('extract'); calculator.extractDoubleStats('hu-0')">匹配0位</button>
                        <button class="stats-vertical-btn" onclick="playSound('extract'); calculator.extractDoubleStats('hu-1')">匹配1位</button>
                        <button class="stats-vertical-btn" onclick="playSound('extract'); calculator.extractDoubleStats('hu-2')">匹配2位</button>
                        <button class="stats-vertical-btn" onclick="playSound('extract'); calculator.extractDoubleStats('hu-01')">匹配01位</button>
                        <button class="stats-vertical-btn" onclick="playSound('extract'); calculator.extractDoubleStats('hu-12')">匹配12位</button>
                    </div>
                    
                    <div class="position-column">
                        <div class="position-title">十个位</div>
                        <button class="stats-vertical-btn" onclick="playSound('extract'); calculator.extractDoubleStats('tu-0')">匹配0位</button>
                        <button class="stats-vertical-btn" onclick="playSound('extract'); calculator.extractDoubleStats('tu-1')">匹配1位</button>
                        <button class="stats-vertical-btn" onclick="playSound('extract'); calculator.extractDoubleStats('tu-2')">匹配2位</button>
                        <button class="stats-vertical-btn" onclick="playSound('extract'); calculator.extractDoubleStats('tu-01')">匹配01位</button>
                        <button class="stats-vertical-btn" onclick="playSound('extract'); calculator.extractDoubleStats('tu-12')">匹配12位</button>
                    </div>
                </div>
                <button class="btn btn-brand" onclick="playSound('copy'); calculator.copyDoubleStats()" style="width: 100%; margin-top: 15px;">复制双位提取</button>
            </div>
            
            <!-- 提取结果显示 -->
            <div id="statsResult" style="display: none; margin-top: 20px;">
                <div class="stats-title" id="statsTitle"></div>
                <div class="group-list" id="statsGroups"></div>
            </div>
        </div>
        
        <!-- 品牌页脚 -->
        <div class="brand-footer">
            <div>© 2023 <span class="brand-highlight">十方料码</span> - 专业彩票数据分析工具</div>
            <div style="margin-top: 5px; font-size: 0.8rem;">微信: shifangliaoma | 数据精准 · 命中率高 · 专业可靠</div>
        </div>
    </div>

    <script>
        class LotteryCalculator {
            constructor() {
                this.groups = [];
                this.selectedMatches = new Set(['2', '23']);
                this.selectedDouble = {
                    position: null,
                    type: null
                };
                this.results = [];
                this.currentExtraction = null;
                this.audioEnabled = true;
                this.init();
            }
            
            init() {
                this.renderMatchOptions();
                this.loadFromStorage();
                this.updateDisplay();
            }
            
            renderMatchOptions() {
                const options = [
                    { value: '0', label: '0', desc: '匹配0位' },
                    { value: '1', label: '1', desc: '匹配1位' },
                    { value: '01', label: '01', desc: '匹配0-1位' },
                    { value: '12', label: '12', desc: '匹配1-2位' },
                    { value: '23', label: '23', desc: '匹配2-3位' },
                    { value: '123', label: '123', desc: '匹配123位' },
                    { value: '3', label: '3', desc: '匹配3位' },
                    { value: '2', label: '2', desc: '匹配2位' }
                ];
                
                const row1 = document.getElementById('matchRow1');
                const row2 = document.getElementById('matchRow2');
                
                row1.innerHTML = options.slice(0, 4).map(opt => `
                    <div class="match-option ${this.selectedMatches.has(opt.value) ? 'selected' : ''}" 
                         onclick="playSound('select'); calculator.toggleMatch('${opt.value}')">
                        <div style="font-weight: 800;">${opt.label}</div>
                        <div style="font-size: 11px; opacity: 0.8;">${opt.desc}</div>
                    </div>
                `).join('');
                
                row2.innerHTML = options.slice(4).map(opt => `
                    <div class="match-option ${this.selectedMatches.has(opt.value) ? 'selected' : ''}" 
                         onclick="playSound('select'); calculator.toggleMatch('${opt.value}')">
                        <div style="font-weight: 800;">${opt.label}</div>
                        <div style="font-size: 11px; opacity: 0.8;">${opt.desc}</div>
                    </div>
                `).join('');
            }
            
            toggleMatch(value) {
                if (this.selectedMatches.has(value)) {
                    this.selectedMatches.delete(value);
                } else {
                    this.selectedMatches.add(value);
                }
                this.renderMatchOptions();
                this.saveToStorage();
                this.speak(`已选择匹配${value}位`);
            }
            
            selectDoublePosition(position) {
                this.selectedDouble.position = position;
                this.updateDoubleUI();
                const positionNames = {
                    'ht': '百十位',
                    'hu': '百个位',
                    'tu': '十个位'
                };
                this.speak(`已选择${positionNames[position]}`);
            }
            
            selectDoubleType(type) {
                this.selectedDouble.type = type;
                this.updateDoubleUI();
                this.speak(`已选择匹配${type}位`);
            }
            
            updateDoubleUI() {
                ['ht', 'hu', 'tu'].forEach(pos => {
                    const btn = document.getElementById(`btn${pos.toUpperCase()}`);
                    if (btn) {
                        btn.classList.toggle('selected', this.selectedDouble.position === pos);
                    }
                });
                
                const typeButtons = document.querySelectorAll('.type-btn');
                typeButtons.forEach(btn => {
                    const type = btn.getAttribute('onclick').match(/'([^']+)'/)[1];
                    btn.classList.toggle('selected', this.selectedDouble.type === type);
                });
            }
            
            importData() {
                const data = document.getElementById('importData').value;
                if (!data.trim()) {
                    this.speak('请输入数据');
                    alert('请输入数据');
                    return 0;
                }
                
                const lines = data.split('\n').filter(line => line.trim());
                let count = 0;
                this.groups = [];
                
                for (const line of lines) {
                    const parts = line.split('-').filter(part => part.trim());
                    if (parts.length === 3) {
                        const [h, t, u] = parts.map(part => {
                            const cleaned = part.replace(/[^0-9]/g, '');
                            return cleaned || '0';
                        });
                        
                        if (h && t && u) {
                            this.groups.push({ h, t, u });
                            count++;
                        }
                    }
                }
                
                this.saveToStorage();
                this.updateDisplay();
                this.speak(`十方料码成功导入${count}组数据`);
                return count;
            }
            
            clearData() {
                this.groups = [];
                document.getElementById('importData').value = '';
                this.results = [];
                document.getElementById('resultsCard').style.display = 'none';
                this.saveToStorage();
                this.updateDisplay();
                this.speak('数据已清除');
            }
            
            updateDisplay() {
                const groupCount = this.groups.length;
                document.getElementById('groupCount').textContent = groupCount;
                document.getElementById('dataStatus').textContent = groupCount > 0 ? '已加载' : '未加载';
                
                if (groupCount > 0) {
                    document.getElementById('dataStatus').style.color = 'var(--primary-green)';
                } else {
                    document.getElementById('dataStatus').style.color = 'var(--primary-red)';
                }
            }
            
            calculate() {
                if (this.groups.length === 0) {
                    this.speak('请先导入数据');
                    alert('请先导入数据');
                    return;
                }
                
                if (this.selectedMatches.size === 0) {
                    this.speak('请选择全局匹配设置');
                    alert('请选择全局匹配设置');
                    return;
                }
                
                const globalMin = parseInt(document.getElementById('globalMin').value) || 0;
                const globalMax = parseInt(document.getElementById('globalMax').value) || 0;
                
                // 应用全局匹配筛选
                this.results = this.groups.filter(group => {
                    // 这里需要实现实际的匹配逻辑
                    // 简化处理：随机模拟匹配结果
                    const randomMatch = Math.floor(Math.random() * 4); // 0-3
                    const matchTypes = Array.from(this.selectedMatches);
                    
                    // 检查随机匹配是否符合任一选中的匹配类型
                    return matchTypes.some(type => {
                        if (type.includes('-')) {
                            // 处理范围类型如"0-1"
                            const [min, max] = type.split('').map(Number);
                            return randomMatch >= min && randomMatch <= max;
                        } else {
                            // 处理单个数字类型如"0", "1", "2", "3"
                            const nums = type.split('').map(Number);
                            return nums.includes(randomMatch);
                        }
                    });
                });
                
                // 如果设置了双位匹配，进一步筛选
                if (this.selectedDouble.position && this.selectedDouble.type) {
                    const doubleMin = parseInt(document.getElementById('doubleMin').value) || 0;
                    const doubleMax = parseInt(document.getElementById('doubleMax').value) || 0;
                    
                    // 简化处理：随机模拟双位匹配结果
                    this.results = this.results.filter(() => {
                        const randomDoubleMatch = Math.floor(Math.random() * 3); // 0-2
                        const type = this.selectedDouble.type;
                        
                        if (type.includes('-')) {
                            // 处理范围类型如"0-1"
                            const [min, max] = type.split('').map(Number);
                            return randomDoubleMatch >= min && randomDoubleMatch <= max;
                        } else {
                            // 处理单个数字类型
                            return randomDoubleMatch === parseInt(type);
                        }
                    });
                }
                
                // 显示结果
                document.getElementById('resultCount').textContent = this.results.length;
                document.getElementById('resultsCard').style.display = 'block';
                
                if (this.results.length > 0) {
                    this.speak(`十方料码计算完成，找到${this.results.length}个符合条件的号码`);
                } else {
                    this.speak('没有找到符合条件的号码');
                }
            }
            
            copyResults() {
                if (this.results.length === 0) {
                    this.speak('没有结果可复制');
                    alert('没有结果可复制');
                    return;
                }
                
                const resultText = this.results.map(g => `${g.h}-${g.t}-${g.u}`).join('\n');
                
                navigator.clipboard.writeText(resultText)
                    .then(() => {
                        this.speak('十方料码结果已复制到剪贴板');
                        alert('结果已复制到剪贴板');
                    })
                    .catch(err => {
                        console.error('复制失败: ', err);
                        this.speak('复制失败');
                    });
            }
            
            verifyWinning() {
                const winningNumber = document.getElementById('winningNumber').value;
                
                if (winningNumber.length !== 3) {
                    this.speak('请输入3位开奖号码');
                    alert('请输入3位开奖号码');
                    return;
                }
                
                if (this.results.length === 0) {
                    this.speak('请先进行计算');
                    alert('请先进行计算');
                    return;
                }
                
                const [h, t, u] = winningNumber.split('');
                let isWinner = false;
                
                // 检查开奖号是否在结果中
                for (const group of this.results) {
                    // 简化验证逻辑：检查每个位置是否匹配
                    const hMatch = group.h.includes(h);
                    const tMatch = group.t.includes(t);
                    const uMatch = group.u.includes(u);
                    
                    if (hMatch && tMatch && uMatch) {
                        isWinner = true;
                        break;
                    }
                }
                
                const resultElement = document.getElementById('verificationResult');
                resultElement.innerHTML = isWinner 
                    ? '🎉 恭喜中奖！十方料码精准命中！' 
                    : '❌ 未中奖，请咨询微信: shifangliaoma 获取更多策略';
                
                resultElement.className = `verification-result ${isWinner ? 'win' : 'lose'} show`;
                
                if (isWinner) {
                    this.speak('恭喜中奖！十方料码精准命中！');
                } else {
                    this.speak('未中奖，请咨询微信shifangliaoma获取更多策略');
                }
            }
            
            extractGlobalStats(type) {
                if (this.groups.length === 0) {
                    this.speak('请先导入数据');
                    alert('请先导入数据');
                    return;
                }
                
                // 模拟提取过程
                const extractedGroups = [];
                const sampleCount = Math.min(10, this.groups.length);
                
                for (let i = 0; i < sampleCount; i++) {
                    const group = this.groups[i];
                    extractedGroups.push(`${group.h}-${group.t}-${group.u}`);
                }
                
                this.currentExtraction = {
                    type: 'global',
                    data: extractedGroups
                };
                
                this.showStatsResult(`全局匹配${type}位`, extractedGroups);
                this.speak(`十方料码已提取全局匹配${type}位数据`);
            }
            
            extractDoubleStats(key) {
                if (this.groups.length === 0) {
                    this.speak('请先导入数据');
                    alert('请先导入数据');
                    return;
                }
                
                const [position, type] = key.split('-');
                const positionNames = {
                    'ht': '百十位',
                    'hu': '百个位',
                    'tu': '十个位'
                };
                
                // 模拟提取过程
                const extractedGroups = [];
                const sampleCount = Math.min(8, this.groups.length);
                
                for (let i = 0; i < sampleCount; i++) {
                    const group = this.groups[i];
                    extractedGroups.push(`${group.h}-${group.t}-${group.u}`);
                }
                
                this.currentExtraction = {
                    type: 'double',
                    data: extractedGroups
                };
                
                this.showStatsResult(`${positionNames[position]}匹配${type}位`, extractedGroups);
                this.speak(`十方料码已提取${positionNames[position]}匹配${type}位数据`);
            }
            
            showStatsResult(title, groups) {
                const statsResult = document.getElementById('statsResult');
                const statsTitle = document.getElementById('statsTitle');
                const statsGroups = document.getElementById('statsGroups');
                
                statsTitle.textContent = `${title} (${groups.length}组) - 十方料码`;
                statsGroups.innerHTML = groups.map(g => `<div class="group-item">${g}</div>`).join('');
                
                statsResult.style.display = 'block';
            }
            
            copyGlobalStats() {
                if (!this.currentExtraction || this.currentExtraction.type !== 'global') {
                    this.speak('请先提取全局统计数据');
                    alert('请先提取全局统计数据');
                    return;
                }
                
                const text = this.currentExtraction.data.join('\n');
                navigator.clipboard.writeText(text)
                    .then(() => {
                        this.speak('十方料码全局统计结果已复制');
                        alert('全局统计结果已复制');
                    })
                    .catch(err => {
                        console.error('复制失败: ', err);
                        this.speak('复制失败');
                    });
            }
            
            copyDoubleStats() {
                if (!this.currentExtraction || this.currentExtraction.type !== 'double') {
                    this.speak('请先提取双位统计数据');
                    alert('请先提取双位统计数据');
                    return;
                }
                
                const text = this.currentExtraction.data.join('\n');
                navigator.clipboard.writeText(text)
                    .then(() => {
                        this.speak('十方料码双位统计结果已复制');
                        alert('双位统计结果已复制');
                    })
                    .catch(err => {
                        console.error('复制失败: ', err);
                        this.speak('复制失败');
                    });
            }
            
            saveToStorage() {
                const data = {
                    groups: this.groups,
                    selectedMatches: Array.from(this.selectedMatches),
                    selectedDouble: this.selectedDouble,
                    results: this.results
                };
                localStorage.setItem('shifangliaoma_calculator', JSON.stringify(data));
            }
            
            loadFromStorage() {
                const saved = localStorage.getItem('shifangliaoma_calculator');
                if (saved) {
                    try {
                        const data = JSON.parse(saved);
                        this.groups = data.groups || [];
                        this.selectedMatches = new Set(data.selectedMatches || ['2', '23']);
                        this.selectedDouble = data.selectedDouble || { position: null, type: null };
                        this.results = data.results || [];
                        
                        this.renderMatchOptions();
                        this.updateDoubleUI();
                        
                        // 更新UI显示
                        if (this.groups.length > 0) {
                            const textData = this.groups.map(g => `${g.h}-${g.t}-${g.u}`).join('\n');
                            document.getElementById('importData').value = textData;
                        }
                        
                        if (this.results.length > 0) {
                            document.getElementById('resultCount').textContent = this.results.length;
                            document.getElementById('resultsCard').style.display = 'block';
                        }
                        
                        this.speak('十方料码数据已从本地存储加载');
                    } catch (e) {
                        console.error('加载存储数据失败:', e);
                    }
                }
            }
            
            speak(text) {
                if (!this.audioEnabled) return;
                
                if ('speechSynthesis' in window) {
                    // 停止当前语音
                    window.speechSynthesis.cancel();
                    
                    const utterance = new SpeechSynthesisUtterance(text);
                    utterance.lang = 'zh-CN';
                    utterance.rate = 1.0;
                    utterance.pitch = 1.0;
                    utterance.volume = 1.0;
                    window.speechSynthesis.speak(utterance);
                }
            }
        }

        // 全局音频控制
        function toggleAudio() {
            const toggleBtn = document.getElementById('audioToggle');
            if (calculator.audioEnabled) {
                calculator.audioEnabled = false;
                toggleBtn.classList.add('muted');
                toggleBtn.textContent = '🔇';
                playSound('mute');
            } else {
                calculator.audioEnabled = true;
                toggleBtn.classList.remove('muted');
                toggleBtn.textContent = '🔊';
                playSound('unmute');
                calculator.speak('十方料码音频已启用');
            }
        }

        // 简单音效函数
        function playSound(type) {
            if (!calculator.audioEnabled) return;
            
            try {
                const audioContext = new (window.AudioContext || window.webkitAudioContext)();
                const oscillator = audioContext.createOscillator();
                const gainNode = audioContext.createGain();
                
                oscillator.connect(gainNode);
                gainNode.connect(audioContext.destination);
                
                // 根据不同操作设置不同音调
                let frequency = 440;
                switch(type) {
                    case 'import': frequency = 523.25; break; // C5
                    case 'calculate': frequency = 659.25; break; // E5
                    case 'select': frequency = 392.00; break; // G4
                    case 'copy': frequency = 493.88; break; // B4
                    case 'verify': frequency = 587.33; break; // D5
                    case 'extract': frequency = 349.23; break; // F4
                    case 'clear': frequency = 293.66; break; // D4
                    case 'mute': frequency = 220.00; break; // A3
                    case 'unmute': frequency = 880.00; break; // A5
                }
                
                oscillator.frequency.setValueAtTime(frequency, audioContext.currentTime);
                gainNode.gain.setValueAtTime(0.1, audioContext.currentTime);
                gainNode.gain.exponentialRampToValueAtTime(0.01, audioContext.currentTime + 0.1);
                
                oscillator.start();
                oscillator.stop(audioContext.currentTime + 0.1);
            } catch (e) {
                console.log('音频播放失败，但功能正常');
            }
        }

        // 初始化计算器实例
        const calculator = new LotteryCalculator();
        
        // 页面加载完成后初始化
        window.addEventListener('load', () => {
            calculator.speak('十方料码伪序动态复式软件已加载，专业彩票数据分析工具');
        });
    </script>
</body>
</html>
